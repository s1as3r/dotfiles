#!/usr/bin/env bash
match_rr() {
  for refresh_rate in $(wlr-randr | rg "(\d*.?\d*) Hz" -r '$1' -oNI); do
    if $(echo $refresh_rate | rg -q "$1"); then
      echo $refresh_rate;
      return;
    fi
  done
}

wlr-randr --output eDP-1 --mode 2560x1600@$(match_rr "$1")
