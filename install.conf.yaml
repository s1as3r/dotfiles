- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.config/fish: fish
    ~/.ghci: ghci
    ~/.config/kitty: kitty
    ~/.config/rofi: rofi
    ~/.Xmodmap: Xmodmap

- shell:
  - [git submodule update --init --recursive, Installing submodules]

