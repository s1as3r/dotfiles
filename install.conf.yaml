- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.config/fish: fish
    ~/.ghci: ghci
    ~/.config/kitty: kitty

- shell:
  - [git submodule update --init --recursive, Installing submodules]

